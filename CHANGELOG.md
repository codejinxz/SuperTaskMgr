# Changelog

本项目的所有重要变更都记录在此文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

（暂无。）

## [1.0.0] - 2026-09-23

首个正式版本。单文件便携 exe（2,914,816 字节 ≈ 2.9 MB、静态 CRT、零第三方 DLL），
覆盖进程监控与控制、内存清理、性能图表、网络、启动项/服务/驱动管理、
硬件传感器与可选内核增强。以下条目如实归纳自 `docs/phase/` 的开发过程记录。

### Added（新增）

- **进程监控**：13 列全量进程表（CPU / 私有工作集 / 提交 / 磁盘 / 网络 / 硬故障 / 句柄 / 线程 / 上下文切换 / 徽标 / 描述），1 秒级刷新、排序、过滤、列拖动重排与列宽持久化；平铺 / 树形视图切换；区分系统进程三态（关闭 / 高亮 / 只看用户进程）；详情面板（路径 / 签名 / 命令行 / 用户名 / GDI+USER / 父进程跳转 / 已加载模块列表 + 按模块签名）。
- **进程控制**：挂起 / 恢复（半文档化 API 动态绑定 + 文档化逐线程回退）、优先级六档（Realtime 档双警示）、CPU 亲和性逐核设置。
- **终止**：单进程 / 进程树终止；树杀两段式（先 Plan 显示"预计 N 个"，执行时重新快照、逐成员身份重验、保护成员跳过并如实报告）。
- **内存**：释放进程工作集、清理系统待机缓存；一键内存优化（Top10 高占用勾选批量释放 + 可选清待机缓存）。
- **性能**：CPU（总+每核）/ 内存 / 磁盘 / 网络 / 硬故障 / 上下文切换 / GPU 等多块实时曲线；时间窗 60/120/300/600 秒可切（切换不清历史）；块放大详查 + 跟随/检视状态机；每核热图；Y2 副轴；拖拽告警阈值线；CSV 记录导出。
- **网络**：TCP/UDP 连接表；适配器信息卡（类型 / IPv4 / IPv6 / 网关 / DNS / MAC / 链路速度 / DHCP，一键复制 IP）；连接级实时监视（事件流 + 过滤 + Top 远程目标 + DNS 记录 + CSV）；按进程流量统计（ETW Kernel-Network，需管理员）；Npcap 深度抓包（含载荷 hex/ASCII 视图，需用户自行安装）；纵向可拖拽分栏。
- **系统管理**：四源启动项枚举与启停（注册表 Run/Run32、启动文件夹、计划任务、UWP StartupTask，写前自动备份可逆）；服务枚举 / 启停（依赖树警告）；驱动列表 + 按需签名校验；崩溃记录页（事件日志 1000/1001/1002 最近 200 条，免管理员）；窗口管理页（前置 / 置顶 / 最小化 / 温和关闭）。
- **硬件传感器**：CPU 每核频率与占用率、ACPI 热区逐实例温度、SMART 磁盘健康细项（NVMe 备件%/磨损%/通电时长、SATA 温度）、GPU 逐传感器展开（NVML 存在时）、每适配器网络吞吐、电池、内存/分页池、风扇；各分组可勾选显示 + 详细模式。
- **可选内核增强（全 opt-in）**：PawnIO 集成——CPU 每核 DTS 温度、主板风扇转速、电压（模块 SHA-256 白名单校验，只读，不篡改风扇策略）；可选 LibreHardwareMonitor 数据源（默认关，仅读本机 127.0.0.1）。
- **外观与易用性**：主题三态（深色 / 浅色 / 跟随系统）；自定义壁纸（PNG/JPG/BMP，遮罩浓度可调）；关于对话框；日志查看器（过滤 / 诊断报告生成 / 打开目录）；兼容模式可点击诊断（逐项测量值 + 重试 + 报告导出）；一键优化布局 / 重置布局；全局热键 `Ctrl+Alt+M`；托盘；阈值告警；标题栏 X 行为三选。
- **工程与验证**：176 项自动化自测（`stm_selftest`）、GUI 无交互冒烟（`--smoke`）、六条真实 ImGui 输入管线注入回归（`--autotest dialogclick|kill|tree|startup|about|wallpaper`）；`--json` 机器可读输出。

### Changed（变更）

- **发布信息集中化**：版本 / 发布时间 / 仓库地址收敛到 `src/app/AboutInfo.h` 三个常量，改一处即可发版。
- **注释全中文化**：约 110 个文件、约 2640 处英文注释翻译为中文（标识符与 API 名保持英文）。
- **内存预算按实测修订**：空载工作集由原定 60 MB 预算修订为"≤150 MB 且无增长趋势，泄漏门禁 = 长期运行增长 <5 MB"（依据实测证据，属诚实修订）。
- **提权重启协议**：UAC 弹窗移入工作线程，UI 不再被阻塞；单实例互斥体等待时间由 1s 调整为 3s（防止提权交接误报）。
- **布局稳定化**：状态栏改用定宽槽位（数值变化零平移）、页面区块用定高滚动区、网络页适配器区与监视/抓包段定高，消除数据驱动的顶栏抖动。

### Fixed（修复）

以下为开发过程中发现并修复的、用户可感知的重要缺陷（按严重度，P0 优先）：

- **确认框点击无反应**（用户报告）：确认模态只在单帧渲染，且每帧抢键盘焦点偷走鼠标按住按钮的 ActiveId。改为模态每帧渲染、焦点仅在 `IsWindowAppearing()` 时调一次，并为确认动作统一走 `ConfirmAction.h`。
- **壁纸永不渲染**：绘制调用位于 `NewFrame()` 之前，背景绘制列表因时间戳机制永不并入 DrawData；同时退出时误删持久化副本。两处均修复。
- **图表整窗画错数据**：ImPlot 的 `spec.Offset` 语义与环形缓冲不匹配，稳态下 7 个图表绘制旧数据；改为先线性化环形缓冲并以 `Offset=0` 绘制。
- **网络监视三重缺陷**：IPv4 事件被 IPv6 差分清空、两个 ETW 功能会话互踢静默失效、远程端口未换字节序（443 显示成 47873）。
- **传感器幻影磁盘 / NVMe+ATA 偏移错误**：`CreateFileW` 失败返回值判断错误虚构约 30 块盘；NVMe 健康日志与 ATA SMART 字段偏移错误（按规范布局重写并加合理域钳制）。
- **手写 PNG 压缩器缺陷**：距离码编码错误导致 256px 资产全损（修正后重新生成资产并解码验证）。
- **关于页运行环境恒显"0 秒"**：改为进程起点稳态时钟 + `RtlGetVersion` 兜底 + 函数级缓存。
- **崩溃记录全是"未知/-"**：重写事件属性解析（单/双引号兼容 + 数值实体 + 无名 blob 启发式 + WER 参数），真实样本固化为回归夹具。
- **工具条右侧挤成一团 / 「关于」无法点击**：改用流式布局 + 实测宽度装箱，抽出 `HeaderLayout.h` 纯函数并用单测钉死无重叠。
- **跨线程 `wantExit` 非原子（UB）**：改为 `std::atomic<bool>`。
- 另有大量 P1 级修复（UAF、句柄/SAFEARRAY 泄漏、竞态、诚实性缺陷、布局抖动等），均记录于 `docs/phase/` 的维护与评审报告。

### Security（安全）

- **破坏性操作双层防线**：UI 二次确认 + ops 层保护名单硬拒绝；保护名单命中要求路径位于 `SystemRoot` 下（防同名伪造绕过），路径未知时保守保护。
- **权限最小化**：`SeDebugPrivilege` 仅在提权实例中按需启用、用后即释，每次启用写日志。
- **无隐蔽能力**：全仓验证无自启动写入、无服务安装、无监听端口、无进程注入 API（`WriteProcessMemory` / `CreateRemoteThread`）。
- **内核组件全 opt-in 且只读**：PawnIO 模块经 SHA-256 白名单校验，SuperIO 进入/退出序列 RAII 配对；Npcap 仅本机抓包、载荷截断。不捆绑、不分发、不自动安装任何驱动。
- **启动项写入可逆**：写前自动备份原值（时间戳命名保留历史；计划任务额外备份 XML）。
- **日志隐私**：日志不记录命令行 / 窗口标题；诊断报告内置隐私提示，不自动上传。

[未发布]: https://github.com/codejinxz/SuperTaskMgr/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/codejinxz/SuperTaskMgr/releases/tag/v1.0.0
