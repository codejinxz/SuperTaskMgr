# 安全策略

本文件说明 SuperTaskMgr（Windows 超级任务管理器）的安全模型、信任边界，以及如何报告安全问题。

---

## 1. 安全模型

### 1.1 定位与信任假设

本工具定位为**面向系统管理员的本地自用工具**，其安全模型建立在以下前提上：

- 使用者是**本机管理员**，对自己的机器有完全控制权。
- 工具本身**不是**安全边界：它读取系统信息、控制进程与服务——这些能力在管理员手中本就是可用的。工具的作用是让这些能力**透明、可审计、有防线**，而不是替代操作系统的访问控制。
- 因此本工具**不防御**"管理员对自己机器的合法操作"；它防御的是**误操作**（破坏性操作的二次确认与保护名单）与**意外数据外泄**（不联网、不上传）。

### 1.2 安全承诺（红线）

以下承诺由实现保证，欢迎审阅与验证：

| 承诺 | 实现 |
|---|---|
| **不联网上传任何数据** | 无对外网络请求；诊断报告只落盘到本地并由用户手动复制/发送，不自动上传。 |
| **无监听端口** | 不创建任何监听套接字（ETW / IP Helper API 仅作数据采集用途）。 |
| **零持久化** | 不写自启动项、不安装服务、不留常驻进程；所有配置只写 `%LOCALAPPDATA%\SuperTaskMgr\`。 |
| **零隐蔽能力** | 无进程注入 API（`WriteProcessMemory` / `CreateRemoteThread`）、无规避检测、无后台潜伏行为。 |
| **内核组件全 opt-in** | 本体**零驱动**；可选内核能力依赖**用户自行安装**的官方签名运行时（PawnIO / Npcap），本工具不捆绑、不分发、不自动安装。 |
| **权限最小化** | 默认 `asInvoker` 启动；`SeDebugPrivilege` 仅在提权实例中按需启用、**用后即释**，每次启用写日志。 |
| **日志隐私** | 日志与诊断报告**不记录命令行与窗口标题**；报告内置隐私提示。 |

> 已知例外：进程崩溃时，管理员权限下已开启的 ETW 会话可能残留约 4 MB 非分页池，直至系统重启或下次同名实例启动时清理。这是 Windows ETW 会话机制的固有行为，已在 README 与代码中如实声明。

---

## 2. 破坏性操作防线

所有破坏性操作（杀进程 / 进程树、释放工作集、清待机缓存、禁用启动项、启停服务）都受**双层防线**保护：

1. **UI 层二次确认**：确认框取消键位于首位，破坏性按钮移出键盘导航（防误按 Enter）。
2. **ops 层保护名单硬拒绝**：`core/ProtectedList.h` 定义系统关键进程名单（PID 0/4 及 csrss / lsass / wininit / winlogon / services / smss / Registry / Memory Compression / dwm 等）。名单命中的进程**不可终止、不可挂起、不可 trim**。

保护名单实现细节（防绕过）：

- 名单命中要求进程路径位于 `SystemRoot` 下，**防止同名伪造进程绕过**；路径未知时**保守保护**。
- 树杀逐成员过保护名单，受保护成员**跳过并如实报告**（不整树拒绝、也不放行）。
- 挂起关键进程比杀死更危险（挂起 csrss 会冻结整个系统），因此挂起/恢复同样受保护名单硬拒。

启动项写入操作**写前自动备份**原值到 `logs/startup_backup/`（时间戳命名保留历史；计划任务额外备份 XML），保证可逆。

---

## 3. 可选内核组件的安全约束

本工具**不内置任何驱动**。可选的深度能力依赖用户自行安装的官方签名运行时：

| 组件 | 用途 | 安全约束 |
|---|---|---|
| **PawnIO** | CPU 每核 DTS 温度、主板风扇转速、电压 | 用户自行安装的官方签名运行时（HVCI / Secure Boot 兼容）；模块经 **SHA-256 白名单校验**后才加载；**只读**访问 MSR / SuperIO，**不篡改风扇策略**；SuperIO 进入/退出序列 RAII 配对，杜绝残留配置态。 |
| **Npcap** | 深度抓包（含载荷） | 用户自行安装的官方签名驱动（Nmap Software LLC）；**仅本机抓包**，载荷截断显示；不做 .pcap 导出。其免费版许可禁止外部再分发，故本仓库不含安装包。 |
| **LibreHardwareMonitor** | 可选传感器数据源（默认关） | 由用户自行运行并开启其 Web 服务器；本工具**仅读本机 `127.0.0.1`**，不与之建立任何对外连接。 |

> 本项目**明确排除** WinRing0 类驱动：它已被微软列入漏洞驱动阻止名单（Vulnerable Driver Blocklist），在启用 HVCI 的系统上无法加载且被 Defender 拦截。选型依据见 [docs/phase/00_技术选型评估报告.md](docs/phase/00_技术选型评估报告.md) 与 [docs/phase/11_kernel_research.md](docs/phase/11_kernel_research.md)。

---

## 4. 未签名程序与你该知道的

本工具以**本地构建**分发，**未申请代码签名**：

- Windows SmartScreen 与部分杀软可能提示"未知发布者"/"未知应用"，这是未签名程序的固有现象。
- 提示**不代表**程序有恶意行为（见 §1.2 承诺清单）。
- 如需核对完整性，请比对 [Releases](https://github.com/codejinxz/SuperTaskMgr/releases) 中提供的 SHA256 校验值：

```bat
certutil -hashfile SuperTaskMgr.exe SHA256
```

---

## 5. 报告安全问题

### 5.1 渠道

- **一般安全问题**：通过 GitHub Issues 报告 — <https://github.com/codejinxz/SuperTaskMgr/issues>
- **敏感问题**（如可利用的绕过保护名单、越界/内存安全问题、权限提升路径）：请**不要**在公开 Issue 中张贴可利用的细节。可先开一个不含细节的 Issue 说明"有一个安全问题想私下沟通"，或使用 GitHub 的 [私密漏洞报告](https://docs.github.com/zh/code-security/security-advisories/guidance-on-reporting-and-writing-information-about-vulnerabilities/privately-reporting-a-security-vulnerability) 功能。

> 本项目为个人开源项目，**未承诺响应时限或漏洞赏金**，但会认真对待每一份报告。

### 5.2 报告内容建议

请尽量包含：

1. **版本与环境**：`SuperTaskMgr` 版本号（关于对话框可见）、Windows 版本与构建号、是否提权运行。
2. **复现步骤**：从启动到触发问题的清晰步骤（越具体越好）。
3. **预期行为 vs 实际行为**。
4. **证据**：日志片段（`%LOCALAPPDATA%\SuperTaskMgr\logs`，「日志」页可导出诊断报告）、截图或最小复现。
5. **影响评估**：可影响的功能、能否导致数据损坏/权限提升/信息泄露等。
6. **可能的修复方向**（如有）。

### 5.3 我们如何处理

- 确认收到后会评估影响范围（对照 §1.2 的承诺清单与 §2 的防线）。
- 若确认为缺陷，会在修复后于 `CHANGELOG.md` 的 **Security** 分类下如实记录（涉及可利用细节时会谨慎披露）。
- 若判定为**设计内行为**（例如"保护名单阻止了某操作"、"普通权限读不到他人进程数据"），会说明设计理由。

---

## 6. 范围内 / 范围外

**我们关心的（范围内）**

- 绕过保护名单或二次确认防线的路径。
- 内存安全问题（越界读写、UAF、泄漏导致的拒绝服务）。
- 权限提升（普通权限下获得本应需提权的能力）。
- 违反 §1.2 承诺的行为（意外的网络请求、意外的持久化写入）。
- 敏感数据意外写入日志或随诊断报告外泄。

**我们视为设计内行为的（范围外）**

- 本工具能终止进程、启停服务、禁用启动项——这些是**管理员自用工具的预期功能**（且有防线与备份）。
- 普通权限下无法读取他人进程的路径/命令行等——这是 Windows 权限模型的正常结果，界面会如实标注"需提权"。
- 未签名带来的 SmartScreen 警告（见 §4）。
- 要求本工具在非 x64 或低于 Windows 10 21H2 的系统上运行。
