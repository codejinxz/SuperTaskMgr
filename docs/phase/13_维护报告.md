# 维护轮报告（13）：图表 Phase B/C/D + Npcap 深度抓包 + 布局稳定化 + 评审修复

- 触发：用户①工具条统一风格（主题/壁纸/关于入口）②全面严格 review ③图表优化由 subagent 评定 ④网络适配器信息（参考任务管理器）+ 其他补充 ⑤代码注释全中文 ⑥上传仓库前准备。中途追加：右下角"帧 xx 毫秒"位置漂移修复、页面顶栏位置固定。
- 执行：多轮 subagent 流水线（A1 工具条统一+关于环境修复 ∥ A2 网络适配器卡 ∥ R-NetData 适配器数据层 ∥ R-ChartEval 图表设计 → A4 图表 Phase A → L1 布局稳定化 ∥ C2 Npcap 续完 → M1 图表 Phase B/C/D ∥ M2 网络/传感器页顶栏固定）→ V21–V28 八名评审（含 V23 独立复核）→ 全部 P0/P1 修复 → 终验。

## 一、交付内容

| 需求 | 落地 |
|---|---|
| 工具条统一风格 | [暂停采集][间隔滑条][提权重启][清理待机缓存][内存加速…][主题…][关于] 全部一级按钮；「主题…」=外观设置模态（主题三态/壁纸/遮罩/恢复列宽）；「关于」=关于模态；删除"外观"下拉与迷你「?」 |
| 布局稳定化 | 状态栏右段定宽槽位（数值变化零平移，StatusLayout.h 纯函数+4 用例）；性能页 GPU 块定高、网格行高恒定；网络页适配器卡区定高 200px 滚动、实时监视/抓包段定高 340px（任意数据变化下"连接表"标题 y 恒定）；传感器页分组区定高滚动（页头稳定）；窗口页计数定宽 |
| 图表 Phase B | 块「放大/还原」（cfg perfZoom）；FollowTick 跟随/检视状态机（纯函数+用例，ImPlot 1.0 Cond_Always 与用户视窗冲突已按源码推演处理）；hover 精确读数（PerfReadoutText，时间标签按真实秒） |
| 图表 Phase C | 磁盘队列长度（PDH，无计数器诚实空态）；内存提交占比 Y2；磁盘队列 Y2；GPU 利用率历史（2s 摄取）；**每核热图**（cfg 默认关，Viridis 0-100）；CSV 增列 |
| 图表 Phase D | GetIfTable2 按 ifIndex 差分 → SystemInfo.netAdapters（防下溢/首拍/回退处理）→ 网络块"每适配器吞吐"多序列图（Top8+总数注明） |
| Npcap 深度抓包 | 网络页「深度抓包」区：设备选择/BPF 过滤/开始停止/包表（五元组+协议+长度+信息）/选中包 hex+ASCII 视图（载荷≤256B）/统计与丢弃计数/CSV 导出（元数据）；NpcapParse.h 纯函数解析（Ethernet/VLAN/IPv4/v6/TCP/UDP/ICMP/回环伪装帧，畸形防越界）；官方安装器下载+签名校验（Nmap Software LLC）已完成，**免费版不支持静默安装（官方 changelog 证实）——请手动双击 `build_c2_npcap\npcap-1.89.exe` 默认安装**，装后网络页「重新检测」即启用 |
| 关于页修复 | 运行时长改为进程起点稳态时钟（原恒显 0 秒）；WindowsBuildText 加 RtlGetVersion 兜底+Server 系统正确标注+函数级缓存（消除每帧注册表读取） |

## 二、评审与修复（V21–V23 及本轮：P0×2、P1×4，全部修复）

| 级别 | 问题 | 修复 |
|---|---|---|
| P0(V21) | PlotRing 直传环形 offset——ImPlot Offset 按"绘制点数"取模，稳态下 7 图整窗画旧数据 | CopyRingTail 线性化 + Offset=0 + 回归用例（复算 mod-count 语义） |
| P1(V23) | 修复引入：X 轴上限未随 tickSec 换算（500ms 档半幅/5000ms 档裁剪） | 7 处 SetupAxisLimits 乘 tickSec（V23 复核判定本体 PASS 后补齐） |
| P1(V27) | 悬停读数"N 秒前"硬编码 tick 数（500ms 档放大 2 倍失实） | PerfReadoutText 增 tickSec 参数；最新样本显示"刚刚" |
| P1(V28) | 网络页适配器区以下仍数据驱动抖动（事件表空态互斥/警告行区外/Top 表生长） | 实时监视整段定高滚动 Child；抓包区同原则 |
| P1(V28) | 窗口页计数推挤按钮 | 定宽槽位（L1 已并入） |
| P2×12 | GPU 块定高、窗口页计数槽、主题重复点击守卫、commitPct 门、热图开关等 | 已采纳落实 |

## 三、终版验证

- Release + Debug 双配置 **0 错误 0 警告**；stm_selftest **146/146 双轮通过**（累计新增约 53 项用例）；`--smoke 150` 通过；**六条注入式回归全 PASS**；单文件 2.85MB；英文注释残留 0（grep 抽查）。
- 历史全部用户报告缺陷（确认框/壁纸/关于/兼容模式/内存加速入口/布局抖动）零复发（V28 逐项代码路径核验）。

## 四、遗留（backlog）
Npcap 抓包真机验证（待用户手动安装驱动后跑 `--autotest` 外的手工验收）；图表 Phase B 的适配器 tooltip 行；崩溃页导出；Find Window；浅色×壁纸对比度增强；PerfCsv 适配器列。

## 五、V29/V30 复审补充（架构师亲修）

V29/V30 复审：P0=0，P1×3（其中两条同一根因）、P2×11。全部修复：

| 级别 | 问题 | 修复 |
|---|---|---|
| P1(V29/V30 同源) | 「重置布局」不清 netcol_/GPU 列宽内存缓存——本次会话不回默认宽，且后续拖列以旧宽写回 cfg（"复活"） | ThemeCfg.h 布局重置代际（LayoutResetGeneration/NotifyLayoutReset）；NetColCache/GpuColCache 记录 loadedGen，代际变化即失效重读 |
| P1(V29) | 退出清理只剔 colW_*，重置软删的 netcol_*/layoutScale/colOrder/perfZoom 空值行残留 config.json（约 35 行/轮） | 退出过滤扩为全布局键（真实值保留） |
| P2 采纳 | 会话恢复窗口矩形钳制到最近显示器工作区（防整窗落屏外）；WM_DISPLAYCHANGE 语义说明（layoutScale 为用户显式设置，不自动跟随——在"一键优化布局"中重按即刷新）；colOrderAppliedGen_ 死字段、AboutUi g_logoContext 死参数、EllipsizeTextUtf8 O(N²)（量级微小）等登记或顺带清理 | 落实/登记 |

 ImGui 1.92.9 重排语义（ColumnUserID 稳定、TableSetColumnDisplayOrder 移动语义、惰性布局）经 V29 对照 vendored 源码逐条核实无误；传感器页收缩策略经反馈环分析判定稳定。
